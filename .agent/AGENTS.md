# Git Workflow Override

For this specific project, disable the global feature branch rule:
- Do NOT create feature branches (`ai/<feature-name>`).
- Work directly in the `main` branch.
- Still ask for review before committing unless specified otherwise.
